// Admin app shell placeholder. Visual direction: `Latest Design/` export
// (dashboard, jobs, officers, summary, payments, billing, reports).
// All data comes from the Hono Web API (NEXT_PUBLIC_API_URL) — this app is
// pure UI; no business logic lives here (PRD v2.2 Section 1.1).

export default function Home() {
  return (
    <main style={{ padding: 48, fontFamily: 'sans-serif' }}>
      <h1>PilotNow Admin</h1>
      <p>Workforce operations for security manpower companies.</p>
    </main>
  );
}
