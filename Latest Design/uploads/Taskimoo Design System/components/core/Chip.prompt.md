Chip — a pill-radius tag for filters and metadata, a step larger than `Pill`.

```jsx
<Chip dot="var(--status-done)">Released</Chip>
<Chip red>Blocked</Chip>
<Chip mono>main</Chip>
```

Notes: reach for `Pill` for table/kanban status; use `Chip` for filter rails and repo/branch tags.
